/* Stili generali */
body {
    font-family: 'Arial', sans-serif;
    background-color: #f0f4f8;
    color: #333;
    margin: 0;
    padding: 20px;
}

/* Titolo */
h2 {
    text-align: center;
    color: #4a90e2;
    font-size: 24px;
    margin-bottom: 20px;
}

/* Stile per il pulsante di caricamento */
button {
    background-color: #ff6b6b; /* Colore vivace rosso-arancio */
    color: white;
    border: none;
    padding: 12px 24px; /* Pulsante più grande e imbottito */
    font-size: 18px; /* Dimensione del testo maggiore */
    border-radius: 10px; /* Raggio di curvatura per renderlo più arrotondato */
    cursor: pointer;
    display: block;
    margin: 20px auto;
    transition: all 0.3s ease; /* Transizione fluida */
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Ombra per dare profondità */
}

button:hover {
    background-color: #ff4757; /* Cambia a un colore rosso più intenso al passaggio del mouse */
    transform: scale(1.05); /* Leggero ingrandimento al passaggio del mouse */
}

/* Div che contiene la lista delle tracce */
#listaTracce {
    margin-top: 30px;
    text-align: left; /* Allinea a sinistra */
}

/* Singola traccia audio */
#audio-wrapper {
    background-color: #ffffff;
    border-radius: 8px;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
    margin: 10px 0; /* Margine superiore e inferiore */
    padding: 5px; /* Ridotto il padding per rendere il riquadro più stretto */
    width: 40%; /* Occupa il 90% della larghezza */
    max-width: 700px; /* Allargato il contenitore */
}

/* Player audio */
audio {
    width: 100%;
    outline: none;
    border-radius: 5px;
}

/* Nome traccia */
#audio-wrapper p {
    font-size: 12px; /* Ridotto per maggiore compattezza */
    font-weight: bold;
    margin-bottom: 3px; /* Ulteriore riduzione dello spazio tra nome e player */
}
